{
    "name": "traffic-monitoring-dashboard",
    "version": "1.0.0",
    "description": "Root orchestrator for starting FastAPI and React dashboard concurrently",
    "scripts": {
        "dev": "concurrently \".\\venv\\Scripts\\python -m uvicorn backend.main:app --reload\" \"npm run dev --prefix frontend\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}